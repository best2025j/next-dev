const express = require('express');
const router = express.Router();

// Get all events
router.get('/')

// Adds a new event
router.post('/')

// Deletes an event, requires ADMIN-ACCESS
router.delete('/:id')

// Updates an event, requires ADMIN-ACCESS
router.patch('/:id')